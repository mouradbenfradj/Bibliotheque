{% extends 'base.html.twig' %}

{% block title %}Reservation{% endblock %}

{% block body %}
    <h1>Reservation</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ reservation.id }}</td>
            </tr>
            <tr>
                <th>ReserverPour</th>
                <td>{{ reservation.reserverPour ? reservation.reserverPour|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>ReserverA</th>
                <td>{{ reservation.reserverA ? reservation.reserverA|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>RenduLe</th>
                <td>{{ reservation.renduLe ? reservation.renduLe|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('reservation_index') }}">back to list</a>

    <a href="{{ path('reservation_edit', {'id': reservation.id}) }}">edit</a>

    {{ include('reservation/_delete_form.html.twig') }}
{% endblock %}
